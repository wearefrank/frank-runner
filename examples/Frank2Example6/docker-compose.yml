services:
  frank2example6:
    image: frankframework/frankframework:latest
    ports:
      - 8080:8080
    volumes:
      - ./configurations:/opt/frank/configurations
      - ./classes:/opt/frank/resources
    environment:
      instance.name: frank2example6
      dtap.stage: LOC
      configurations.directory.autoLoad: true
